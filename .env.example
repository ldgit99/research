PORT=3000
NODE_ENV=development

# Set exactly one of the two password options below.
# Recommended: bcrypt hash (generate once and store only hash)
DASHBOARD_PASSWORD_HASH=

# Fallback for quick local testing only (do not use in production)
# DASHBOARD_PASSWORD=

JWT_SECRET=replace-with-a-long-random-secret
SESSION_HOURS=8
RESEARCH_AGENT_URL=https://research-agent-ldgit99.streamlit.app/
